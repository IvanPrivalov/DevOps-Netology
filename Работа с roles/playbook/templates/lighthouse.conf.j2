server {
    listen 80 default_server;
    server_name _;

    root {{ lighthouse_install_dir }}/{{ lighthouse_archive_dir }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}